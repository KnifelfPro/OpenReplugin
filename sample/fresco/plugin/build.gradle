plugins {
    id 'com.android.application'
    id 'io.github.knifelfpro.replugin-plugin-gradle'
}

android {
    namespace 'club.knifelf.fresco.plugin'
    compileSdk 37
    defaultConfig {
        applicationId "club.knifelf.fresco.plugin"
        minSdkVersion 21
        targetSdkVersion 37
        versionCode 1
        versionName "1.0"
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
    buildFeatures {
        buildConfig true
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
    lint {
        abortOnError false
    }
}

repluginPluginConfig {
    pluginName = "fresco"
    hostApplicationId = "club.knifelf.fresco.host"
    hostAppLauncherActivity = "club.knifelf.fresco.host.MainActivity"
}

dependencies {
    implementation 'io.github.knifelfpro:replugin-plugin-lib:3.2.0'
    implementation 'androidx.appcompat:appcompat:1.6.1'
    implementation 'com.facebook.fresco:fresco:3.6.0'
}

tasks.register('copyPluginToHost') {
    group = 'sample'
    description = 'Copy plugin debug APK into host assets/plugins as fresco.jar'
    dependsOn 'assembleDebug'
    doLast {
        def apk = fileTree(layout.buildDirectory.dir('outputs/apk/debug')).matching {
            include '*.apk'
            exclude '*unaligned*', '*unsigned*'
        }.files.find { it.name.endsWith('.apk') }
        if (apk == null || !apk.exists()) {
            throw new GradleException('plugin debug apk not found')
        }
        def destDir = file("${rootProject.projectDir}/host/src/main/assets/plugins")
        destDir.mkdirs()
        def dest = new File(destDir, 'fresco.jar')
        dest.bytes = apk.bytes
        println ">>> copied ${apk} -> ${dest}"
    }
}
